%p#notice= notice
%h1= bookmark.title

%p
  %strong Bookmark short url:
  = link_to "#{bookmark.short_url(bookmark.url)}", bookmark.url 

%p
  %strong Bookmark full url:
  = link_to "#{bookmark.url}", bookmark.url 
  
%p
  %strong Tags:
  = bookmark.all_tags

= link_to 'Edit', edit_bookmark_path(bookmark)
|
\#{link_to 'Back', bookmarks_path}